services:
  karmicbotfarm:
    buildCommand: pip install -r requirements.txt
    startCommand: streamlit run streamlit_app.py --server.port=8080 --server.address=0.0.0.0
    envVars:
      - key: STREAMLIT_BROWSER_GATHER_USAGE_STATS
        value: "false"
    startPort: 8080
    healthCheckPath: /
